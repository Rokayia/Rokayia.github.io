<div class="pill2"><slot /></div>

<style>
	.pill2 {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--accent-text-over);
		border: 1px solid var(--accent-light);
		background-color: var(--accent-light);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: wrap;
	}
  
</style>
